export type Account = {
  bankCode: string;
  bankName: string;
  accountNo: string;
  accountName: string;
};

export type Card = {
  cardIssuerCode: string;
  cardIssuerName: string;
  cardNo: string;
  cardName: string;
};
